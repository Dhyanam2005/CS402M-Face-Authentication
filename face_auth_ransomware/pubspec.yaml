name: face_auth_ransomware
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  
  camera: ^0.10.5+9
  tflite_flutter: ^0.12.0
  image: ^4.1.7
  path_provider: ^2.1.3
  permission_handler: ^11.3.0
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.0.0
  encrypt: ^5.0.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/face_model.tflite
    - assets/face_classifier_labels.txt